function add7 (n) {
    return n + 7;
}

function multiply (a, b) {
    return a * b;
}

function capitalize(str) {
    return str = str.charAt(0).toUpperCase() + str.slice(1);
}

function lastLetter(str) {
    return str.slice(-1);
}

console.log(lastLetter("eyuo3ddde"));